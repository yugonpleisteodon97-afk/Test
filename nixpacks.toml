[phases.setup]
nixPkgs = ['python311', 'postgresql', 'gcc', 'g++', 'libpq']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 "app:create_app()"'
